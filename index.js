import React frim 'react';
import ReactDOM from 'react-dom'
import {BrowserRouter} frim 'react-router-dom';
import App from './App';

ReactDOM.render(
  <BrowserRouter>
     <App />
   </BrowserRouter>
  document.getElementyById('root')
);